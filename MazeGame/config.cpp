#include "config.h"

// Intialize the global variables

int CELL_SIZE = 0; // Default cell size (can be changed based on difficulty)
int GRID_WIDTH = 0; // Default grid width (can be changed based on difficulty)
int GRID_HEIGHT = 0; // Default grid height (can be changed based on difficulty)
